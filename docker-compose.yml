version: '3.8'

services:
  flight-deck:
    build: .
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "healthcheck.js"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s