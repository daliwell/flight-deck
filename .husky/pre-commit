#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run all automated tests before commit (unit + frontend + integration + API)
echo "🧪 Running pre-commit tests..."
npm run test:unit && npm run test:api

# Note: Integration tests are skipped in pre-commit (require live APIs)
# Run full test suite with: npm test

# ⚠️  WARNING: Bypassing this hook with --no-verify is NOT recommended
# GitHub Actions will enforce tests on push/PR regardless
